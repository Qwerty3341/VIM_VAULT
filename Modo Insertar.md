```
i = insert
I = insert (lo mismo que el i pero al comienso de una línea)
a = after (hace lo mismo que insert pero después del puntero)
A = After (lo mismo que after pero al final de toda la línea)
```
