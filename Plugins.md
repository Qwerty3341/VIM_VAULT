# Repositorio de VIM Plug
https://github.com/junegunn/vim-plug

# Instalando plugins
En el `.vimrc`
```
" la ruta se pone por default si no ponemos nada
call plug#begin('~/.vim/plugged')

call plug#end()
```